package commppetterm.gui.page;

import org.jetbrains.annotations.NotNull;

public final class WeekView implements Controller {
    @Override
    public @NotNull String path() {
        return "WeekView.fxml";
    }
}
